//pipe
pipe(pipe_fd) + pid = fork() 

//give pid to exec->pid

//bin
pid = fork()

//if (exec-pid == -1) fork() + assign pid to exec->pid()

//fd 3 & 4
if dup() at init_std
//fixed
initializing infile & outfile at -1 + dup2() with STDS

//pending when redir, output = redir

//